name: Deploy to AlwaysData

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy via SSH
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── Set up SSH key ───────────────────────────────────────────────────────
      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo "${{ secrets.SSH_KNOWN_HOSTS }}" > ~/.ssh/known_hosts

      # ── Sync files to the server ─────────────────────────────────────────────
      # Excludes secrets, git metadata and dev-only files.
      - name: Sync application files
        run: |
          rsync -avz --delete \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='config/config.php' \
            --exclude='*.md' \
            --exclude='.env*' \
            . ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.DEPLOY_PATH }}

      # ── Run database migrations ──────────────────────────────────────────────
      - name: Apply database schema
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "psql ${{ secrets.DATABASE_URL }} -f ${{ secrets.DEPLOY_PATH }}/db/schema.sql"

      # ── Seed game system data (idempotent – uses ON CONFLICT DO NOTHING) ─────
      - name: Seed EHP game system
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "psql ${{ secrets.DATABASE_URL }} -f ${{ secrets.DEPLOY_PATH }}/db/seeds/game_system_ehp.sql"

      - name: Deployment complete
        run: echo "✅ Deployment finished successfully."
